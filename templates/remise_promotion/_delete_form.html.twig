<form method="post" action="{{ path('app_remise_promotion_delete', {'id': remise_promotion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ remise_promotion.id) }}">
    <button class="btn">Delete</button>
</form>
