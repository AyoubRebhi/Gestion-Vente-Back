{% extends 'base.html.twig' %}

{% block title %}Edit RemisePromotion{% endblock %}

{% block body %}
    <h1>Edit RemisePromotion</h1>

    {{ include('remise_promotion/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_remise_promotion_index') }}">back to list</a>

    {{ include('remise_promotion/_delete_form.html.twig') }}
{% endblock %}
